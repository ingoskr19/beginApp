import React, { Component } from 'react';
import Nav from './navigator';
class NativeBaseScene extends Component {
  render() {
    return (
      <Nav/>
    );
  }
}

export default NativeBaseScene;
